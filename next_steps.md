# Next Steps

Use this checklist alongside `work_log.md`.

## Currently Hot

- No active threads.

## Background / Paused

No scientific analysis thread is currently active. Reopen an archived workflow only with a concrete analysis question and update `correlation_analysis_writeup.md` if it becomes part of the reported result set.

The treaty content is current and validated. Run `treaty update` only if the maintainer wants Copier metadata advanced from the recorded `v0.2.0` source revision; `.copier-answers.yml` remains Copier-owned.
